export default function Menu() {
  // const fold = " basis-0 w-0";
  // const spread = " basis-60";

  return (
    <section
      className={
        "py-4 gap-2 flex flex-col transition-all duration-300 overflow-auto basis-60"
        // + (isMenu ? spread : fold)
      }
    >
    </section>
  );
}
